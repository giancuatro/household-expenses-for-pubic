node_modules
.next
out
.env
.env.local
.DS_Store
*.log
.vercel
next-env.d.ts
tsconfig.tsbuildinfo
.claude/

# Personal credit-card statements (PDFs / CSVs) used for one-off backfills.
# Never commit financial documents.
credit_statement/

# Review CSV (extracted statement transactions). Keep notes/scripts in repo,
# but the data itself stays local — it includes merchant names + amounts.
review/unmatched-*.csv

# Service Worker (generated by serwist on build)
public/sw.js
public/sw.js.map
public/swe-worker-*.js
public/workbox-*.js
